@model IEnumerable<HastaneOtomasyonu.Models.Sehir>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.adi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.plakaKodu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.telefonKodu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ulke)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.adi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.plakaKodu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.telefonKodu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ulke.adi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
